# UNITI v0.001a17 Text Integrity Alpha Completion Snapshot

Captured: 2026-09-02
Branch: `main`
Implementation checkpoint: `c016007`
Version: `v0.001a17` / `0.1a17`
Remote baseline: `origin/main` at `052f733`

## Delivered boundary

a17 made encoding, BOM, byte order, line endings, malformed bytes, Save/Save As identity, and save-state transitions explicit and verified. The canonical 11 exact profiles and independent EOL policy now flow through inspection, `Document`, status, menus, dialogs, staging, verification, and reopen.

Input uncertainty or malformed preview evidence is serious and modal; mixed EOL is separate, modeless, and never normalized automatically. Every committed output is staged and reread before atomic replacement. Different-path Save As preserves the source tab, opens or reloads one target tab, and enforces overwrite, encoding-change, dirty-target, open-target, and destination-race protections.

## Verification boundary

```text
full pytest: 552 passed, 4 skipped in 14.40s
deep self-check: pass; text-integrity=pass
offscreen smoke: core_ok=true; gui_ok=true; qt_platform=offscreen
native smoke: core_ok=true; gui_ok=true; qt_platform=cocoa
native window shown/closed: true/true
eight-format disposable dogfood: pass with SHA-256 and second reopen
```

Dogfood covered UTF-8 no BOM/CRLF, UTF-8 BOM/LF, Windows-1252/CRLF, UTF-16 LE no BOM, UTF-16 BE BOM, UTF-32 LE BOM, mixed EOL, and malformed UTF-8. Each valid UI case returned one search match before edit and passed Save, Save As byte equality, and exact-profile reopen. The malformed case retained its invalid source byte under same-profile preserve. No qualifying defect remained.

## Continuation

The milestone, design, and execution plan are retained under [`03_implemented`](../../03_implemented/README.md). `v0.001a18` Large-File Alpha becomes active; a19 through a23 remain queued. File-type profiles and syntax highlighting remain parked. No tag or push was performed as part of this snapshot.
